# Blog post drafts

Drafts for promoting PixBible on Blogger. Nothing here has been published — copy the HTML into
Blogger's HTML view yourself, upload the images, and swap the placeholders.

## 2026-07-28 — iOS launch announcement

- Post body: [2026-07-28-pixbible-ios-launch.html](2026-07-28-pixbible-ios-launch.html)
- Language: English (targets the same audience as the `/en` web routes)

### Suggested title

> PixBible: Memorize the KJV Like a Game — Now on iOS

Alternatives, if you want something less product-y:

- Three Ways to Memorize a Bible Verse (and an App That Does All Three)
- Why I Built a Bible Memory App That Looks Like a Game Boy

### Images

Upload from `images/` in this order. The post body has placeholders that must be replaced
with the Blogger-hosted URLs after upload:

| File | Placeholder in HTML | Alt text | Where |
|---|---|---|---|
| `images/01-header.png` | — | PixBible — memorize KJV Bible verses | Post thumbnail / header (set via Blogger, not in body) |
| `images/02-mode-tiles.png` | `IMAGE_02` | already written in the `alt` attribute | Tile arrangement section |
| `images/03-mode-typing.png` | `IMAGE_03` | already written in the `alt` attribute | Typing section |
| `images/04-mode-dictation.png` | `IMAGE_04` | already written in the `alt` attribute | Dictation section |
| `images/05-icon.png` | — | PixBible app icon | Optional — author profile or inline |

Sources: `docs/store-assets/` (English screenshot set `kjv-modes-en-*`, already used for the
App Store listing, so the blog stays visually consistent with the store page).

### Labels / tags

`Bible memory`, `KJV`, `Scripture memorization`, `iOS app`, `Bible study`, `English learning`

### Search description (Blogger's meta description field)

> PixBible turns Bible memorization into a game: arrange word tiles, fill in the blanks, then
> write the verse from memory. All 31,102 KJV verses, free to start on iOS.

## Claims policy

Everything in the draft is verifiable against the shipped app. Specifically **not** claimed,
because it would be false or unverifiable today:

- Download counts, ratings, or user reviews — the app launched too recently
- Android / Google Play availability — not released
- "Typing Bible reading" (성경 타자 통독) — spec exists, feature does not
  (see [status-2026-07-28-aso-screenshots.md](../status-2026-07-28-aso-screenshots.md))

The "surprised some early users" line in the English-practice section is the one soft claim —
delete it if you don't have actual user feedback saying that.
