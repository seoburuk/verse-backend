# 카드 등급 비주얼·승급 연출 강화 설계 (2026-07-31)

카드 도감의 등급 구분이 테두리 색·노치·핍 정도로 은은하다. 등급 비주얼을 더 강하게 만들고,
등급이 오르는 순간(뒤집기 연출)을 더 체감되게 개선한다.

- **대상**: `verse-flutter/` 앱 전용(도감). 오프라인 우선. 새 데이터·테이블 없음.
- **선행**: `2026-07-23-card-collection-design.md`(도감), `2026-07-24-card-tiers-design.md`(등급),
  `2026-07-30-card-catalog-expansion-design.md`(29장 카탈로그). 전부 구현 완료.
- **아트**: **카드 그림(내용물)은 이번에도 범위 밖**이다. 다만 등급 **프레임** 아트는 이번에
  범위 안으로 들어온다(§2-1) — 카드가 무엇을 보여주는지와 무관하게 테두리 장식만 다루므로
  기존 "카드 아트 없음" 제약과 충돌하지 않는다.

---

## 1. 방향 — 정적 강화 + 탭 트리거 반짝임 + 레전드 전용 풀스크린

네 갈래를 조합한다:

1. **정적 레이어 강화(A1)** — 그리드에 가만히 있을 때도 등급 차이가 한눈에 보이게, 실버부터
   재질(디더링)을 더하고 골드·레전드는 장식 요소를 쌓는다. `CustomPainter` 기반이라 아트 없이 동작한다.
2. **프레임 아트 에셋(A3)** — §2의 `CustomPainter` 프레임 위에, 있으면 SVG 프레임 이미지로
   교체한다. 카드 카탈로그가 `image` 파일 유무로 플레이스홀더/실아트를 가르는 것과 같은 패턴을
   프레임에도 적용한다 — **파일이 없으면 A1 페인터가 그대로 폴백**이라 아트 제작이 늦어져도 기능은 완결된다.
3. **탭 트리거 반짝임(A2 변형)** — 상시 애니메이션은 두지 않는다(그리드에 카드가 많아지면
   산만하고 배터리 비용도 있다). 대신 **카드를 탭하는 순간**에만 골드·레전드가 반짝이거나
   파티클을 한 번 재생한다 — "만졌을 때 반응하는" 손맛.
4. **승급 연출 차등(B1) + 레전드 전용 풀스크린(B2)** — 획득 연출(`CardFlip`)에서 등급업이면
   이전 등급 프레임이 잠깐 보인 뒤 새 등급으로 전이되는 것을 보여준다(B1). **레전드로 승급할
   때만** 이 인라인 전이 대신 풀스크린 축하 모달을 띄운다(B2) — 레전드는 도감에서 가장 드물고
   비중 있는 사건이므로 흐름을 끊을 값어치가 있다고 보되, 그 아래 등급은 완료 화면 흐름을
   유지해 방해하지 않는다.

색·모양·개수로 등급을 구별하는 기존 원칙(색약 대응)은 그대로 유지한다. 애니메이션은
그 위에 얹는 장식이지 유일한 신호가 아니다.

---

## 2. 정적 레이어 — `CardTierFrame` 확장

`card_tile.dart`의 `CardTierFrame` 내부에 레이어를 추가한다. 노치·핍·테두리 두께는
기존 그대로 두고 아래를 더한다.

| 등급 | 기존 | 추가 |
|---|---|---|
| 브론즈 | 단색 테두리 2px, 핍 0 | 변경 없음 |
| 실버 | 테두리 2px, 핍 2 | 테두리를 **2색 디더링**(밝은 회색 `#C8CDD6` / 어두운 회색 `#7C8290` 체커 픽셀, 2px 격자)으로 |
| 골드 | 노치 4, 테두리 2.5px, 핍 3 | 디더링(금색 톤) + **모서리 ㄱ자 브래킷**(4px 픽셀 선, 네 모서리 안쪽) + 콘텐츠 배경에 등급색 틴트(alpha 0.12) |
| 레전드 | 노치 7, 테두리 3px, 핍 4, 정지 광택 줄 | 디더링(보라 톤) + 브래킷 + 틴트 + **2중 테두리**(바깥 테두리 안쪽에 1px 간격 두고 얇은 두 번째 선) |

- 디더링·브래킷은 `CustomPainter` 신규 클래스(`_TierDitherPainter`, `_TierBracketPainter`)로 그린다.
  에셋 파일 없음.
- 레전드의 기존 대각선 광택 줄(`_ShinePainter`)은 **정지 상태로 유지**한다(상시 애니메이션 아님 —
  §1의 "상시 애니메이션 없음" 원칙과 일관되게, 이미 있던 정지 장식이라 그대로 둔다).
- 골드·레전드만 디더링+브래킷을 받는다. 브론즈·실버는 디더링만(실버) 또는 아무것도(브론즈) —
  단계가 올라갈수록 레이어가 누적되는 형태라 등급 사다리가 시각적으로도 계단식이다.

---

## 2-1. 프레임 아트 에셋 (A3) — 있으면 대체, 없으면 폴백

`assets/cards/frames/{tier}.svg` (예: `bronze.svg`, `silver.svg`, `gold.svg`, `legend.svg`)를
찾는다. **카드 카탈로그의 `image` 필드와 동일한 패턴**이다 — 카드 도감 스펙 §5가 이미 이 방식을
쓰고 있으므로 새 개념이 아니다.

- 파일이 있으면 `CardTierFrame`이 §2의 `CustomPainter` 레이어 대신 `SvgPicture.asset`으로
  프레임을 그린다. 카드 내용(그림 또는 이름 첫 글자 타일)은 그 위에 그대로 얹힌다 —
  프레임과 콘텐츠는 별개 레이어이므로 카드 아트가 준비되든 안 되든 무관하게 동작한다.
- **파일이 없으면 §2의 페인터가 그대로 렌더**된다(코드 변경 없이 자동). 4개 등급 SVG를 한 번에
  다 만들 필요가 없다 — 하나씩 채워도 나머지는 계속 페인터로 동작한다.
- 노치·핍의 좌표 규칙(§2)은 SVG 프레임에도 동일하게 지켜져야 한다 — 그래야 프레임을 나중에
  교체해도 색약 대응(모양·개수 신호)이 깨지지 않는다. SVG 제작 시 지켜야 할 제약으로 문서화만
  해두고, 강제 검증은 하지 않는다(런타임에 SVG 내부 구조를 검사할 수 없다).
- `assets/cards/frames/`를 `pubspec.yaml` 에셋 목록에 추가한다(폴더 자체는 이번에 비어 있어도
  등록해 둔다 — 나중에 파일만 넣으면 코드 수정 없이 교체된다는 도감 스펙의 원칙을 그대로 잇는다).

---

## 3. 탭 트리거 반짝임 — `CardTile`

`CardTile`은 현재 `StatelessWidget`이고 `onTap`으로 상세 시트를 연다. 이를
`StatefulWidget`으로 바꾸지 않고, **탭 시 반짝임 오버레이를 잠깐 띄우는 방식**으로 구현한다.

- **대상**: 골드·레전드 카드만. 브론즈·실버는 탭해도 지금처럼 바로 상세가 열린다
  (반짝일 장식이 없으므로 지연을 줄 이유가 없다).
- **동작**: 탭하면 `onTap`(상세 열기)은 그대로 즉시 실행하되, **탭 지점에 오버레이 애니메이션을
  얹어 250ms 정도 재생**한다. 상세 시트가 열리는 것을 막거나 늦추지 않는다 — 반짝임은 탭한
  카드 타일 자체 위에서 짧게 스치고 사라지는 부가 효과다.
  - **골드**: 대각선 반짝임 줄이 한 번 타일을 가로질러 지나간다(레전드의 정지 광택과 같은 각도,
    애니메이션 버전).
  - **레전드**: 위 + 픽셀 사각 파티클 6~8개가 카드 중심에서 바깥으로 짧게 튀었다 사라진다.
- 구현: `AnimationController`(단발, 재생 후 자동 dispose 불필요 — 위젯 살아있는 동안 재사용)를
  가진 얇은 래퍼(`_TapSparkle`)로 `CardTile`을 감싼다. 탭마다 `controller.forward(from: 0)`.
- 그리드가 이미 `ListView`라 화면 밖 카드는 애니메이션이 도는 일 자체가 없다(빌드 안 됨).
  상시 애니메이션이 아니므로 화면에 여러 장 있어도 idle 비용이 없다.

---

## 4. 승급 연출 — `CardFlip` 차등 + 레전드 풀스크린

`card_repository.dart`의 `revealedTier()`가 이전 등급을 이미 제공한다 — **새 데이터 없음.**
`CardFlip`에 `prevTier` (`CardTier?`) 파라미터를 추가한다. 호출부(`memorize_screen.dart`)에서
`CardStatus`를 만들 때 이미 `revealedTier`를 조회하므로 그 값을 넘기기만 하면 된다.

**인라인 전이 (브론즈~골드 승급, B1)**

- **`prevTier == null`(첫 획득/브론즈)**: 기존 그대로 — 뒤집으면 바로 최종 프레임.
- **`prevTier != null && 새 등급 != legend`(실버·골드 승급)**: 뒤집기 완료 후,
  1. 먼저 **이전 등급 프레임**으로 앞면이 나타난다.
  2. 400ms 뒤 테두리 색·노치·핍이 **새 등급으로 전이**된다(핍은 늘어난 개수만큼 하나씩
     순차로 찍힌다, 각 80ms 간격).
  3. `HapticFeedback.mediumImpact()` 한 번(전이 시작 시점).
- 여러 장이 동시에 승급해도(한 절이 여러 카드에 걸림) 지금처럼 `CardFlip`이 나란히 나열되고,
  각자 독립적으로 전이 연출을 재생한다 — 순서를 맞추거나 지연시키지 않는다.

**레전드 승급 — 풀스크린 모달 (B2)**

레전드로 오르는 카드는 인라인 전이 대신 **완료 화면 위에 뜨는 풀스크린 축하 모달**로 처리한다.

- 완료 화면(`SectionCompleteScreen`/`memorize_screen.dart`의 결과 화면)이 뜬 뒤, `_cardUpgrades`
  중 새 등급이 `legend`인 항목이 있으면 **그 화면 전체를 덮는 모달**을 띄운다. 나머지(브론즈~골드)
  카드는 기존처럼 `CardFlip` 목록에 남아 인라인으로 처리된다 — 레전드만 격을 높인다.
- 모달 내용: 어두운 배경 위에 카드가 크게 뒤집히며(§2의 강화된 레전드 프레임 + §3의 파티클을
  더 크게) 등장, 이름과 "레전드 달성" 문구, 확인 버튼으로 닫는다. `HapticFeedback.heavyImpact()`.
- **동시에 여러 장이 레전드로 오르면 모달을 큐로 순차 표시**한다(한 번에 하나씩, 닫으면 다음).
  모달을 동시에 여러 개 겹쳐 띄우지 않는다 — 이게 처음에 B2를 보류했던 "모달 스팸" 우려를
  해소하는 지점이다. 레전드는 흔치 않은 사건이라(§3의 클램프로도 도달에 시간이 걸림) 실사용에서
  큐가 2개 이상 쌓이는 일은 드물 것으로 본다.
- 모달을 다 닫으면 완료 화면의 나머지 흐름(인라인 카드 목록, 공유 등)이 이어진다 — 완료 화면
  자체를 대체하지 않고 그 위에 얹히는 오버레이다.

---

## 5. 재사용 관계

| 필요한 것 | 출처 |
|---|---|
| 이전 등급 | 기존 `CardRepository.revealedTier()` (신규 조회 없음, 이미 `_cardUpgrades` 계산 시 호출됨) |
| 등급별 색·라벨 | 기존 `cardTierColor` / `cardTierLabel` |
| 디더링·브래킷·파티클 페인터 | 신규 `CustomPainter` 3종, `card_tile.dart` 내부 |
| 프레임 SVG 폴백 판단 | 기존 "이미지 없으면 플레이스홀더" 패턴(카드 도감 스펙 §5)과 동일 구조 |
| 탭 반짝임 트리거 | 신규 `_TapSparkle` 래퍼, `card_tile.dart` 내부 |
| 승급 전이 상태 | `CardFlip`의 기존 `AnimationController`에 두 번째 구간(전이) 추가 |
| 레전드 모달 큐 | 신규 위젯 `LegendPromotionOverlay`, 완료 화면에서 `_cardUpgrades` 필터링 후 호출 |

---

## 6. 테스트

- **디더링/브래킷 렌더**: 등급별로 기대하는 레이어(브론즈 없음 / 실버 디더링만 / 골드·레전드
  디더링+브래킷)가 위젯 트리에 존재한다. 모르는 `kind`·등급 조합에서 크래시 없다.
- **프레임 SVG 폴백**: `assets/cards/frames/{tier}.svg`가 없으면 페인터 프레임이 렌더된다.
  파일이 있으면 `SvgPicture.asset`으로 대체되고 카드 콘텐츠(그림/이니셜)는 그 위에 그대로 보인다.
- **탭 반짝임 트리거 범위**: 브론즈·실버 카드는 탭 시 반짝임 오버레이가 재생되지 않는다.
  골드·레전드는 탭할 때만 재생되고, 탭 전에는 정지 상태다(상시 애니메이션 아님을 확인).
- **탭해도 상세가 열림**: 반짝임 재생 여부와 무관하게 `onTap` 콜백이 항상 즉시 호출된다
  (반짝임이 상세 열기를 막거나 지연시키지 않는다).
- **`CardFlip` 인라인 전이**: `prevTier == null`이면 기존과 동일하게 즉시 최종 프레임. 실버·골드
  승급이면 이전 등급 프레임이 먼저 나타났다가 새 등급으로 바뀐다. **레전드 승급 카드는 인라인
  목록에서 제외되고 모달로만 처리된다**(중복 노출 없음).
- **레전드 모달 큐**: 동시에 레전드 승급 카드가 2장 이상이면 모달이 한 번에 하나씩만 뜨고,
  닫을 때마다 다음 카드가 이어진다. 완료 화면의 나머지 요소(공유 버튼 등)는 모달 뒤에서 그대로
  유지되고, 모달을 다 닫으면 정상 노출된다.
- **기존 회귀**: 도감·등급 판정·카드 카탈로그 테스트 전부 그대로 통과.

---

## 7. 범위 밖

- **책장(`BookshelfView`) 개선** — 이번 스펙은 카드 도감만 다룬다.
- **정렬·분류 토글(갈래별 필터 등)** — 카탈로그 확장 스펙 §9-3에 남겨둔 후속 과제.
- **도감 NEW/UP 뱃지** — 별도 후속 검토.
- **카드 그림(내용물) 아트 제작** — 계속 플레이스홀더. §2-1의 프레임 아트와는 별개다.
- **상시 반짝임/움직이는 배경** — 의도적으로 배제(§1).
- **브론즈·실버·골드 승급의 풀스크린화** — 이 등급들은 인라인 전이(B1)로 유지한다.
  풀스크린은 레전드 전용이다.

---

## 참고

- 선행 스펙: `docs/superpowers/specs/2026-07-23-card-collection-design.md`,
  `docs/superpowers/specs/2026-07-24-card-tiers-design.md`,
  `docs/superpowers/specs/2026-07-30-card-catalog-expansion-design.md`
- 기존 코드: `lib/features/cards/card_tile.dart`(`CardTierFrame`, §2·§2-1·§3 대상),
  `lib/features/cards/card_flip.dart`(§4 대상), `lib/core/cards/card_repository.dart`
  (`revealedTier`/`markRevealedTier`), `lib/features/memorize/memorize_screen.dart`
  (`_cardUpgrades` 계산부, `CardFlip` 호출부 및 §4 레전드 모달 삽입 지점),
  `pubspec.yaml`(§2-1 `assets/cards/frames/` 등록 대상)
